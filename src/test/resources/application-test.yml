# Test Configuration
spring:
  application:
    name: clown-frontoffice-test
  
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  
  h2:
    console:
      enabled: true

server:
  port: 0

logging:
  level:
    root: WARN
    com.clown: DEBUG
